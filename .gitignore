# Environment variables — never commit
.env
.env.*
.env-server

# SSH keys — never commit
id_rsa
id_rsa.pub
*.ppk

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# n8n workflow exports — local tooling only
n8n-*.json

# Dev-only — not needed on server
README.md
web-team-brief.docx
cover-email.txt
office-planner.html
docs/
daily/
scripts/
